diff --git a/hyprpm/CMakeLists.txt b/hyprpm/CMakeLists.txt
index 1234567..abcdefg 100644
--- a/hyprpm/CMakeLists.txt
+++ b/hyprpm/CMakeLists.txt
@@ -9,7 +9,7 @@ file(GLOB_RECURSE SRCFILES CONFIGURE_DEPENDS "src/*.cpp")
 
 set(CMAKE_CXX_STANDARD 23)
 
-pkg_check_modules(hyprpm_deps REQUIRED IMPORTED_TARGET tomlplusplus hyprutils>=0.7.0)
+pkg_check_modules(hyprpm_deps REQUIRED IMPORTED_TARGET re2 tomlplusplus hyprutils>=0.7.0)
 
 find_package(glaze 7.0.0 QUIET)
 if (NOT glaze_FOUND)
diff --git a/hyprpm/src/core/PluginManager.cpp b/hyprpm/src/core/PluginManager.cpp
index 1234567..abcdefg 100644
--- a/hyprpm/src/core/PluginManager.cpp
+++ b/hyprpm/src/core/PluginManager.cpp
@@ -16,12 +16,15 @@
 #include <fstream>
 #include <algorithm>
 #include <format>
+#include <unordered_map>
+#include <type_traits>
 
 #include <sys/types.h>
 #include <sys/stat.h>
 #include <pwd.h>
 #include <unistd.h>
 
+#include <re2/re2.h>
 #include <toml++/toml.hpp>
 #include <glaze/glaze.hpp>
 
@@ -32,16 +35,23 @@ using namespace Hyprutils::String;
 using namespace Hyprutils::OS;
 using namespace Hyprutils::Memory;
 
-static std::string execAndGet(std::string cmd) {
+template<typename C, typename = std::enable_if_t<
+    std::is_same_v<typename C::value_type, std::pair<const std::string, std::string>>>
+> static std::string execAndGet(std::string cmd, C const &env_vars) {
     cmd += " 2>&1";
 
     CProcess proc("/bin/sh", {"-c", cmd});
+    for (const auto &[k, v] : env_vars)
+        proc.addEnv(k, v);
 
     if (!proc.runSync())
         return "error";
 
     return proc.stdOut();
 }
+static inline std::string execAndGet(std::string cmd) {
+    return execAndGet(cmd, std::vector<std::pair<const std::string, std::string> >());
+}
 
 static std::string getTempRoot() {
     static auto ENV = getenv("XDG_RUNTIME_DIR");
@@ -62,6 +72,27 @@ CPluginManager::CPluginManager() {
     m_szUsername = getpwuid(NSys::getUID())->pw_name;
 }
 
+std::unordered_map<std::string, std::string> CPluginManager::getEnvVars(CProgressBar* progress) {
+    static const std::filesystem::path env_file("/etc/portage/make.conf");
+    std::unordered_map<std::string, std::string> vars;
+    if (std::filesystem::is_regular_file(env_file)) {
+        if (progress)
+            progress->printMessageAbove(infoString("Reading {}", env_file.string()));
+        CProcess proc("/bin/bash", {"-exc", "cd \"$1\"; . \"$2\"", "bash", env_file.parent_path().string(), env_file.filename().string()});
+        if (!proc.runSync()) {
+            std::println(stderr, "\n{}", failureString("Couldn't read {}", env_file.string()));
+            return vars;
+        }
+        re2::StringPiece input(proc.stdErr());
+        std::string name, quoted_value, value;
+        const RE2 re(R"((?m)^(?:\++ )?((?i:[a-z_]\w*))=(?:'((?s:.*?))'|((?:[!-&(-~].*)?))$)");
+        while (RE2::FindAndConsume(&input, re, &name, &quoted_value, &value))
+            if (!getenv(name.c_str()))
+                vars[name] = quoted_value.empty() ? value : quoted_value;
+    }
+    return vars;
+}
+
 SHyprlandVersion CPluginManager::getHyprlandVersion(bool running) {
     static bool             onceRunning   = false;
     static bool             onceInstalled = false;
@@ -300,6 +331,8 @@ bool CPluginManager::addNewPluginRepo(const std::string& url, const std::string&
     progress.printMessageAbove(successString("Hyprland headers OK"));
     progress.m_szCurrentMessage = "Building plugin(s)";
     progress.print();
+
+    const auto vars = getEnvVars(&progress);
 
     for (auto& p : pManifest->m_plugins) {
         std::string out;
@@ -321,7 +354,7 @@ bool CPluginManager::addNewPluginRepo(const std::string& url, const std::string&
                 break;
             }
 
-            out += " -> " + *CMD_RAW + "\n" + execAndGet(*CMD_RAW) + "\n";
+            out += " -> " + *CMD_RAW + "\n" + execAndGet(*CMD_RAW, vars) + "\n";
         }
 
         if (m_bVerbose)
@@ -556,6 +589,8 @@ bool CPluginManager::updateHeaders(bool force) {
 
     progress.printMessageAbove(statusString("!", Colors::YELLOW, "configuring Hyprland"));
 
+    const auto vars = getEnvVars(&progress);
+
     if (m_bVerbose)
         progress.printMessageAbove(verboseString("setting PREFIX for cmake to {}", DataState::getHeadersPath()));
 
@@ -569,7 +604,7 @@ bool CPluginManager::updateHeaders(bool force) {
         return false;
     }
 
-    ret = execAndGet(*CONFIGURE_CMD);
+    ret = execAndGet(*CONFIGURE_CMD, vars);
     if (m_bVerbose)
         progress.printMessageAbove(verboseString("cmake returned: {}", ret));
 
@@ -756,6 +791,8 @@ bool CPluginManager::updatePlugins(bool forceUpdateAll) {
             }
         }
 
+        const auto vars = getEnvVars(&progress);
+
         for (auto& p : pManifest->m_plugins) {
             std::string out;
 
@@ -777,7 +814,7 @@ bool CPluginManager::updatePlugins(bool forceUpdateAll) {
                     break;
                 }
 
-                out += " -> " + *CMD_RAW + "\n" + execAndGet(*CMD_RAW) + "\n";
+                out += " -> " + *CMD_RAW + "\n" + execAndGet(*CMD_RAW, vars) + "\n";
             }
 
             if (m_bVerbose)
diff --git a/hyprpm/src/core/PluginManager.hpp b/hyprpm/src/core/PluginManager.hpp
index 1234567..abcdefg 100644
--- a/hyprpm/src/core/PluginManager.hpp
+++ b/hyprpm/src/core/PluginManager.hpp
@@ -4,6 +4,7 @@
 #include <memory>
 #include <optional>
 #include <string>
+#include <unordered_map>
 #include <expected>
 #include "Plugin.hpp"
 
@@ -84,6 +85,7 @@ class CPluginManager {
     std::string                             headerError(const eHeadersErrors err);
     std::string                             headerErrorShort(const eHeadersErrors err);
     bool                                    validArg(const std::string& s);
+    std::unordered_map<std::string, std::string> getEnvVars(class CProgressBar* progress);
 
     std::expected<std::string, std::string> nixDevelopIfNeeded(const std::string& cmd, const SHyprlandVersion& ver);
 
